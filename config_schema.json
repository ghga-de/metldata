{
  "title": "ModSettings",
  "description": "Modifies the orginal Settings class provided by the user",
  "type": "object",
  "properties": {
    "source_event_topic": {
      "title": "Source Event Topic",
      "description": "Name of the topic to which source events are published.",
      "default": "source_events",
      "env_names": [
        "metl_sub_source_event_topic"
      ],
      "type": "string"
    },
    "source_event_type": {
      "title": "Source Event Type",
      "description": "Name of the event type for source events.",
      "default": "source_event",
      "env_names": [
        "metl_sub_source_event_type"
      ],
      "type": "string"
    },
    "metadata_model_path": {
      "title": "Metadata Model Path",
      "description": "The path to the metadata model defined in LinkML.",
      "env_names": [
        "metl_sub_metadata_model_path"
      ],
      "type": "string",
      "format": "path"
    },
    "submission_store_dir": {
      "title": "Submission Store Dir",
      "description": "The directory where the submission JSONs will be stored.",
      "env_names": [
        "metl_sub_submission_store_dir"
      ],
      "type": "string",
      "format": "path"
    },
    "accession_store_path": {
      "title": "Accession Store Path",
      "description": "A file for storing the already registered accessions.",
      "env_names": [
        "metl_sub_accession_store_path"
      ],
      "type": "string",
      "format": "path"
    },
    "prefix_mapping": {
      "title": "Prefix Mapping",
      "description": "Specifies the ID prefix (values) per resource type (keys).",
      "example": {
        "file": "GHGAF",
        "experiment": "GHGAE",
        "sample": "GHGAS",
        "dataset": "GHGAD"
      },
      "env_names": [
        "metl_sub_prefix_mapping"
      ],
      "type": "object",
      "additionalProperties": {
        "type": "string"
      }
    },
    "suffix_length": {
      "title": "Suffix Length",
      "description": "Length of the numeric ID suffix.",
      "default": 8,
      "env_names": [
        "metl_sub_suffix_length"
      ],
      "type": "integer"
    }
  },
  "required": [
    "metadata_model_path",
    "submission_store_dir",
    "accession_store_path",
    "prefix_mapping"
  ],
  "additionalProperties": false
}