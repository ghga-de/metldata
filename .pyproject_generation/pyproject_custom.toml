[project]
name = "metldata"
version = "3.0.3"
description = "metldata - A framework for handling metadata based on ETL, CQRS, and event sourcing."
dependencies = [
    "hexkit[akafka,mongodb] >=5.1, <6",
    "ghga-service-commons[api,auth] >=4, <5",
    "ghga-event-schemas >=4.1, <5",
    "typer>=0.12",
    "linkml==1.6.1",
    "linkml-runtime==1.6.0",
    "linkml-validator==0.4.5",
    "cryptography>=42.0.0"
]

[project.urls]
Repository = "https://github.com/ghga-de/metldata"

[project.scripts]
metldata = "metldata.__main__:run"
